# Supabase

Run migrations in `supabase/migrations` to create demo-ready Hanooot ERP tables. RLS is enabled; policies are permissive placeholders for local demos and should be tightened when auth roles are connected.
