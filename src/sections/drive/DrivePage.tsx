"use client"
import { FormEvent, useState } from "react"
import { Badge, Button, Card, PageHeader, SelectPill } from "@/components/erp/ui"
import { driveRows } from "@/data/erp-data"
export const DrivePage = () => { const [rows,setRows]=useState(driveRows); const submit=(e:FormEvent)=>{e.preventDefault(); setRows([["pdf","Uploaded supplier certificate.pdf","Product · CN-24110 · Today","You","316 KB"],...rows])}; return <><PageHeader eyebrow="Storage" title="Drive" subtitle="Upload/add file workflow and storage table with Name, Linked to, Owner, and Size columns." actions={<><SelectPill>All folders</SelectPill><Button>Upload</Button></>} /><Card className="mb-4"><form onSubmit={submit} className="flex flex-wrap items-end gap-3"><label className="text-sm font-semibold">Upload to<br/><input defaultValue="Sourcing · CN-24110" className="mt-1 rounded-xl border p-2" /></label><label className="text-sm font-semibold">File<br/><input defaultValue="supplier certificate.pdf" className="mt-1 rounded-xl border p-2" /></label><Button type="submit">Add file</Button><Badge tone="slate">Supabase Storage boundary</Badge></form></Card><Card><table className="w-full text-left text-sm"><thead className="text-xs uppercase text-slate-400"><tr>{["Ext","Name","Linked to","Owner","Size"].map(h=><th key={h} className="p-3">{h}</th>)}</tr></thead><tbody>{rows.map(row=><tr key={row.join(":")} className="border-t border-slate-100">{row.map((cell,i)=><td key={cell} dir="auto" className="p-3">{i===0?<Badge>{cell}</Badge>:cell}</td>)}</tr>)}</tbody></table></Card></> }
