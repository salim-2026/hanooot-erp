"use client"
import { Badge, Button, Card, PageHeader, SelectPill } from "@/components/erp/ui"
import { contacts } from "@/data/erp-data"
export const ContactsPage = () => <><PageHeader eyebrow="CRM" title="Contacts" subtitle="Deduped by phone; every lead ever synced can be selected for CSV export, Facebook Custom Audience, copy phones, and bulk WhatsApp." actions={<><SelectPill>Customers</SelectPill><SelectPill>Suppliers</SelectPill><Button>Export CSV for FB audience</Button><Button tone="secondary">Bulk WhatsApp</Button></>} /><Card><table className="w-full text-left text-sm"><thead className="text-xs uppercase text-slate-400"><tr>{["Contact", "Phone", "City", "Source", "Orders", "Total spent", "Status"].map((h)=><th key={h} className="p-3">{h}</th>)}</tr></thead><tbody>{contacts.map((row)=><tr key={row.join(":")} className="border-t border-slate-100">{row.map((cell,i)=><td key={cell} dir="auto" className="p-3">{i===6?<Badge tone="green">{cell}</Badge>:cell}</td>)}</tr>)}</tbody></table></Card></>
