"use client"
import { useState } from "react"
import { Badge, Button, Card, Drawer, PageHeader } from "@/components/erp/ui"
import { hrPeople } from "@/data/erp-data"
export const HrPage = () => { const [approved,setApproved]=useState(false); const person=hrPeople[1]; return <><PageHeader eyebrow="Human resources" title="People" subtitle="Headcount, employee table, leave approvals, payroll run, gross/tax/net columns, and employee documents." actions={<Button>+ Add employee</Button>} /><div className="grid gap-4 xl:grid-cols-[1fr_360px]"><div className="space-y-4"><div className="grid gap-4 md:grid-cols-3">{[["Headcount","18","+2 this month"],["Leave approvals",approved?"0":"1","pending decision"],["Payroll run","$48.2k","August 2026"]].map(k=><Card key={k[0]}><p className="text-xs uppercase text-[#78736a]">{k[0]}</p><p className="mt-2 text-3xl font-bold">{k[1]}</p><p className="text-sm text-[#78736a]">{k[2]}</p></Card>)}</div><Card><table className="w-full text-left text-sm"><thead className="text-xs uppercase text-[#8d877c]"><tr>{["Employee","Title","Department","Status","Gross","Net"].map(h=><th key={h} className="p-3">{h}</th>)}</tr></thead><tbody>{hrPeople.map(row=><tr key={row.join(":")} className="border-t border-[#e4e0d6]">{row.map((cell,i)=><td key={cell} dir="auto" className="p-3">{i===3?<Badge tone={cell.includes("pending")?"amber":"green"}>{cell}</Badge>:cell}</td>)}</tr>)}</tbody></table></Card><Card><h2 className="font-bold">Leave approvals</h2><p dir="auto" className="mt-2 text-sm">سارة منصور · Annual leave · Sep 21–24 · 4 days</p><div className="mt-3 flex gap-2"><Button onClick={()=>setApproved(true)}>Approve</Button><Button tone="danger">Decline</Button></div></Card></div><Drawer title={`${person[0]} · ${person[1]}`}><p>Contact · sara@hanooot.example</p><p>Annual leave · {approved?"Approved by you":"Pending approval"}</p><p>Salary — monthly: Gross {person[4]} · Net {person[5]}</p><p>Documents: contract.pdf · visa.pdf</p></Drawer></div></> }
